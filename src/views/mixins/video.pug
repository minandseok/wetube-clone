mixin video(video)
    div.video-box
        div.video-thumb
        div.video-sub
            h4
                a(href=`/video/${video.id}`)=video.title
            small=video.date
            small #{video.meta.views} 회