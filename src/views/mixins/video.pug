mixin video(video)
    div.video-box
        img.video-thumb(src=video.thumbUrl crossorigin="anonymous" style=`background-size:cover;background-position:center;`)

        div.video-sub
            h4
                a(href=`/video/${video.id}`)=video.title
            small=video.date
            small #{video.meta.views} 회